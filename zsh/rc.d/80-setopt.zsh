# Enable spellcheck
setopt correct

# No beep
setopt no_beep
setopt no_list_beep
setopt no_hist_beep

# Also search command from subdirectory in $PATH.
# You can split a command by '/' to specify relative path from $PATH.
setopt path_dirs

# Enable multi redirection
setopt multios
